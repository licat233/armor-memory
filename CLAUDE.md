# CLAUDE

## ARMOR Minimal Stable

For persistent ARMOR Vault document operations:

- Load `.agent/skills/armor-memory/SKILL.md`.
- Use the shared classification protocol.
- Do not construct destination paths directly.
- Call `.agent/skills/armor-memory/scripts/route.sh`.
- Use exactly the returned path.
- Draft and unverified are statuses, not directories.
- Ordinary document writes are route -> write -> stop.

